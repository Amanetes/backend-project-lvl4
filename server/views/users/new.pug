extends ../layouts/application.pug

block content
	.row.justify-content-center
		.card.shadow-sm.bg-light.bg-gradient.rounded-3
			.card-body.row.p-4
			.col-12.col-md-6.d-flex.align-items-center
			.form-signin.text-center
				i.bi.bi-person-square(style='font-size: 3rem; color: DodgerBlue;')
				h1.h3.mb-3.fw-normal= t('views.users.new.signUp')
				+formFor()(action=route('session') method='post')
					+input(user, 'Имя', t('views.placeholders.firstName'), errors)(type='text')
					+input(user, 'Фамилия', t('views.placeholders.lastName'), errors)(type='text')
					+input(user, 'Email', t('views.placeholders.email'),  errors)(type='text')
					+input(user, 'Пароль', t('views.placeholders.password'), errors)(type='password')
					+submit(user)(value=t('views.users.new.submit'))
					p.mt-3.mb-3.text-muted &copy; 2022 Hexlet Ltd.